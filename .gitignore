# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (can opt-in for committing if needed)
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# storybook
/storybook-static

# Design exports are a functional dependency here, not incidental reference
# material: .storybook/main.ts serves references/flows via staticDirs so the
# reference-overlay QA harness works on a fresh checkout and in CI. Keep them
# tracked (~33MB).

# Local Netlify folder
.netlify
dist-app/
screenshots/

# Raw POS capture: ~134MB of full-resolution product photography. The servable
# set is built from it into public/pos-images/ (1.2MB) by
# scripts/build-pos-images.mjs, and that output IS committed. Keeping the raw
# capture out keeps clones and the Pages deploy small.
/references/pos-item-imagery
